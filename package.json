{
  "dependencies": {
    "elm": "^0.19.0-no-deps",
    "elm-format": "^0.8.1",
    "elm-live": "^3.4.1"
  },
  "scripts": {
    "server": "elm-live src/Main.elm -u -s index.html -- --output=app.js --debug",
    "s": "yarn run server",
    "fmt": "elm-format"
  }
}
